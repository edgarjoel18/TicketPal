/** Fake implementation instead of running nats-wrapper. We want to redirect nats to a fake server */
export const natsWrapper = {
  client: {
    publish: jest
      .fn()
      .mockImplementation(
        (subject: string, data: string, callback: () => void) => {
          callback();
        }
      ),
  },
};
